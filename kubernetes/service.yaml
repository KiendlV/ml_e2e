apiVersion: v1
kind: Service

metadata:
  name: rain-predictor-service

spec:
  selector:
    app: rain-predictor

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: LoadBalancer